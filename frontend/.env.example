# Browser requests stay on the frontend origin so authentication cookies work.
NEXT_PUBLIC_API_BASE_URL=/api
# Server-side rewrite destination. On Vercel, use the Render backend URL.
API_BACKEND_URL=http://localhost:5002
